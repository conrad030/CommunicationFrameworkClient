# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'CommunicationFramework' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CommunicationFramework
  pod 'AzureCommunicationCalling', '~> 2.0.0'
  pod 'AzureNotificationHubs-iOS'
  pod 'AzureCommunicationChat'

  target 'CommunicationFrameworkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CommunicationFrameworkUITests' do
    # Pods for testing
  end

end
